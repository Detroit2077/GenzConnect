import React from "react";
import LoginComponent from "../Login";

const Login: React.FC = () => {
    return (
        <div>
            <LoginComponent />
        </div>
    );
};

export default Login;
